# 이번에 뭘 고쳤고, 앞으로 뭘 하면 되는지

`stabilize/phase-0` 브랜치에서 작업했습니다. 커밋 6개입니다.

먼저 말씀드리면, **코드 상태가 생각보다 훨씬 좋습니다.** 다시 만들 필요 없습니다.
`app/admin/app.js`에 함수가 1,209개 있는데 100줄 넘는 건 33개뿐입니다.
함수를 잘게 쪼개는 습관이 이미 잡혀 있다는 뜻입니다. 전역 이름 충돌도 0건이고,
`escapeHtml` 같은 안전장치도 이미 670번 넘게 쓰고 있고, 비밀키가 올라간 것도 없습니다.
`scripts/check_cloudflare_build.py`는 잘 만든 안전장치입니다.

문제는 **코드 실력이 아니라 파일 정리와 배포 절차**였습니다. 성격이 다른 문제고,
기계적으로 고칠 수 있습니다.

---

## 이번에 고친 것

### 1. 회원 이름이 관리자 화면에서 코드로 실행될 수 있었습니다

회원이 직접 수정하는 이름·닉네임·거주동·메모가 이스케이프 없이 화면에 그려지고
있었습니다. 회원이 자기 이름을 스크립트로 바꿔두면, **관리자가 그 회원 목록을 여는
순간 관리자 권한으로 그 코드가 실행됩니다.** 계정을 뺏길 수 있는 문제입니다.

사용자 입력이 HTML로 들어가는 13곳을 전부 `escapeHtml`로 감쌌습니다.
(`schedule-v2-admin.js`는 이미 전부 처리돼 있어서 손대지 않았습니다.)

### 2. 관리자만 옛날 커리큘럼을 보고 있었습니다

관리자 화면은 커리큘럼 파일을 `?v=notion-catalog-2`로, 회원·코치 앱은 `-3`으로
요청하고 있었습니다. 같은 파일인데 캐시 키가 달라서 관리자 브라우저만 예전
카탈로그를 계속 붙들 수 있었습니다. `-3`으로 맞췄습니다.

### 3. 접속할 때마다 앱 전체를 다시 받고 있었습니다

모든 파일에 `no-store`(저장 금지)가 걸려 있어서, 관리자 2.3MB · 회원앱 1.1MB를
매번 통째로 다시 받았습니다. 캐시를 깨려고 `?v=` 문자열을 73곳에 손으로 관리하는데,
정작 캐시가 꺼져 있어서 그 관리가 아무 의미가 없는 상태였습니다.

`no-cache`(저장하되 매번 확인)로 바꿨습니다. **안 바뀐 파일은 "그대로임" 응답만
받고 끝나므로, 옛 코드가 남을 위험은 여전히 0이고 전송량만 사라집니다.**
모바일에서 체감이 클 겁니다.

보안 헤더도 같이 넣었습니다 — 클릭재킹 방지, base 태그 주입 방지 등.

### 4. 배포 전에 못 잡던 것들을 CI가 잡게 했습니다

이번에 찾은 문제가 **둘 다 CI를 그냥 통과했었습니다.** 검사 범위가 좁았기 때문입니다.

- 링크 검사: `<script>`와 `<link>`만 보고 `<a>`는 안 봤습니다 → `<a>`, `<img>`까지,
  그리고 배포본 안의 모든 HTML로 넓혔습니다.
- 버전 검사: 아예 없었습니다 → `?v=` 값과 `tennisnote-release.js`가
  `release.json`과 다르면 실패합니다.

일부러 버전을 어긋나게 하고, 일부러 링크를 깨뜨려서 **둘 다 실제로 잡히는 것까지
확인했습니다.**

그리고 이 검사가 **새 버그를 하나 더 잡았습니다.** 관리자 사이트에도 약관·방침
페이지가 같이 올라가는데, 그 페이지의 "앱으로 돌아가기" 버튼이 회원앱 경로를
가리켜서 404였습니다(관리자 쪽엔 회원앱이 없으니까요). 회원 사이트로 넘기게 했습니다.

### 5. 로컬에서 고쳐도 화면이 안 바뀌던 문제

`localhost`에서도 서비스워커가 등록돼서, 파일을 고쳐도 캐시된 옛 파일이 나오고
`releaseId`가 바뀔 때마다 페이지가 새로고침돼 작업이 끊겼습니다.
로컬에서는 서비스워커를 등록하지 않고, 이미 깔린 것도 정리하도록 했습니다.
**배포 환경 동작은 그대로입니다.**

### 6. AGENTS.md

Codex로 개발하는데 규칙 파일이 없어서, 매 세션마다 관례를 다시 추측하고 배포
체크리스트를 잊는 상태였습니다. `AGENTS.md`에 규칙을 적어뒀습니다.
**앞으로 Codex한테 일 시킬 때 이 파일이 알아서 읽힙니다.**

---

## 일부러 안 건드린 것

**약관·개인정보처리방침은 손대지 않았습니다.** 사업자 정보가 필요하고 법적 검토가
따라야 해서, 제가 만들 수 있는 게 아닙니다. 다만 문제가 있다는 건 알아두셔야 합니다.

- **가입 화면의 「서비스 이용약관」 링크가 404입니다.** 커밋 460개를 다 뒤졌는데
  그 파일은 한 번도 존재한 적이 없습니다. 지금 가입하는 모든 회원이 **열어볼 수 없는
  문서에 필수 동의**를 하고 있습니다. 전자상거래법상 약관은 동의 전에 제시돼야 해서
  단순 링크 오류가 아닙니다. 이게 가장 급합니다.
- **개인정보처리방침이 두 벌이고, 앱은 초안을 보여줍니다.** 루트의 `privacy.html`은
  시행일 7/18 정식본으로 개인정보 처리자·연락처·주소가 다 있습니다. 그런데 앱이 실제로
  링크하는 `app/tennis-note-legal/privacy.html`은 7/17 **초안**이고, "베타 테스트용
  초안"이라는 안내문이 그대로 있고, 8번 항목에 처리자와 연락처가 아예 없습니다.
  수집 항목 설명도 서로 반대로 적혀 있습니다.

두 번째는 정식본을 앱 쪽으로 복사하고 링크만 정리하면 되는 일이라 어렵지 않습니다.
첫 번째는 약관 본문이 필요합니다.

---

## 앞으로 뭘 하면 되는지

**순서가 내용보다 중요합니다.** 테스트도 없는 상태에서 31,000줄을 먼저 쪼개면
뭔가 조용히 깨진 채로 배포되고, 되돌릴 기록도 없습니다.

### 지금 (며칠 안)

1. **약관 파일 만들고 가입 화면 링크 연결.** 만들고 나면
   `scripts/check_cloudflare_build.py`의 `KNOWN_MISSING_PAGES`에서 그 줄을 지우세요.
   지우지 않으면 검사가 계속 통과합니다.
2. **개인정보처리방침 한 벌로 통합.** 루트 정식본을 기준으로.

### 다음 (1일)

3. **`scripts/bump_release.py` 만들기.** 버전 번호 하나만 받아서 `release.json`
   기준으로 73곳과 서비스워커 캐시 이름 2개를 전부 갱신하는 스크립트입니다.
   **배포할 때마다 10개 파일을 손으로 고치는 일이 사라집니다.**
   지금은 CI가 불일치를 잡아주긴 하지만, 애초에 틀릴 일이 없게 만드는 게 낫습니다.

4. **커밋 습관 바꾸기.** 지금 460개 커밋 중 380개가 `Publish Tennis Note PWA 1.0.x`
   한 줄입니다. 어떤 커밋은 12개 파일에 1,500줄이 들어 있습니다. 그래서 **"이게 언제
   왜 깨졌지"를 물어볼 방법이 없고**, 기능 하나만 되돌릴 수도 없습니다.

   변경 하나에 커밋 하나, 제목은 사람이 읽을 수 있게. 배포 버전 올리기는 따로.
   이번 브랜치의 커밋 6개가 그 예시입니다.

### 그다음 (1~2주, 천천히)

5. **파일 쪼개기.** `<script type="module">`로 바꾸고 화면 단위로 나눕니다.
   관리자 앱은 이미 화면이 정확히 10개(`dashboardView`, `scheduleView`, `billingView`,
   `membersView`, `settingsView` …)라 그게 그대로 경계선입니다. 파일당 2,000~3,000줄.

   **한 번에 화면 하나씩.** 하나 옮기고 → 로컬에서 눌러보고 → 반영 → 다음.
   절대 두 개를 동시에 하지 마세요. 가장 작은 화면부터 시작해서 방법을 싸게 익히세요.

   2,385줄짜리 `bindEvents()`도 이때 같이 쪼갭니다. 각 화면이 자기 이벤트만 맡게요.

6. **중복 제거.** 회원앱과 코치앱에 이름까지 같은 함수가 32개, 10줄 단위로 똑같은
   블록이 211개입니다. 한쪽에서 버그를 고쳐도 다른 쪽은 그대로 깨져 있습니다.
   `app/shared/`로 옮기세요. **파일을 쪼갠 다음에 하는 게 훨씬 쉽습니다.**

7. **색상 정리.** 세 스타일시트가 각자 `:root`를 선언하고 값이 어긋나 있습니다.
   `--ink`가 관리자는 `#172033`, 회원·코치는 `#182230`. `--wash`는 세 개 다 다릅니다.

### 언젠가

8. **서버 쪽을 볼 수 있게 하기.** 브라우저가 `tn_users` 같은 테이블에 직접 접근하는
   구조라 **보안은 전적으로 Supabase의 RLS 정책이 책임집니다.** 그런데 그 정책과
   엣지 함수 8개가 다른 저장소에 있어서, 이 저장소만 봐서는 "이 앱이 안전한가"를
   판단할 수 없습니다. 읽기 전용 사본이라도 가져와두면 좋습니다.

---

## 참고: 남아 있는 자잘한 것들

- `app/shared/vendor/xlsx.full.min.js`가 없어서 엑셀 기능이 항상 외부 CDN에
  의존합니다. 로컬 사본을 두려던 흔적인데 파일이 안 들어왔습니다.
- 루트의 `_headers` 파일은 GitHub Pages에서 무시됩니다. 실제로 적용되는 건
  빌드가 생성하는 쪽입니다. 헷갈릴 수 있어 적어둡니다.
- `localStorage` 키가 35개인데 정리된 목록이 없습니다.
  `tennis-note-admin-layout-v1`과 `tennisnote_admin_layout_v1`처럼 규칙이
  어긋난 것도 있습니다.
- `renderAll()`이 클릭 한 번에 화면 전체를 다시 그립니다. 호출이 123곳입니다.
  목록이 길어지면 느려지고 입력 커서가 튀는 원인입니다. 급하진 않습니다.

---

## 확인 방법

```bash
# 화면 띄우기 — 설치할 것도, 키를 넣을 것도 없습니다
python3 -m http.server 8000
# 회원앱  http://localhost:8000/app/tennis-note-member-app/
# 코치앱  http://localhost:8000/app/tennis-note-coach-app/
# 관리자  http://localhost:8000/app/admin/
```

`config.local.js` 404는 정상입니다. 설정이 없으면 데모 모드로 떨어져서
**실제 회원 데이터에 접속하지 않으니 마음껏 만져도 됩니다.**
로그인 없이 보려면 관리자는 `?demoAdmin=1`, 회원앱 커리큘럼은 `?curriculumPreview=1`.

배포 전 검사는 `AGENTS.md`의 "작업 전후에 돌릴 것"에 적어뒀습니다.
브랜치에 올리는 것만으로는 배포되지 않습니다. 배포는 `main`에서만 돕니다.
