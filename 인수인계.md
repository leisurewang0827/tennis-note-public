# 인수인계

일요일 설명용 문서입니다. **이야기가 끝나고 결정이 나면 이 파일은 지우세요.**
정하기 전에 [코덱스 쪽에 물어볼 것](#먼저-코덱스-쪽에-물어볼-것) 부터 보세요.
지켜야 할 규칙은 [AGENTS.md](AGENTS.md) 와 `docs/` 에 있습니다. 여기에는
"무엇을 왜 했는지" 와 "무엇을 정해야 하는지" 만 적습니다.

---

## 한 줄로

세 앱의 `app.js` 가 각각 3만·1만4천·8천 줄이라 사람도 에이전트도 통째로 읽을 수
없었습니다. **동작을 하나도 바꾸지 않고** 역할별 파일로 나눴습니다.

## 지금 상태

| | 시작할 때 | 지금 |
|---|---|---|
| 관리자 `app.js` | 31,096줄 | **1,115줄** |
| 회원앱 `app.js` | 14,218줄 | **972줄** |
| 코치앱 `app.js` | 7,998줄 | **240줄** |
| 자동 검사 | 126개 | **271개** |

그 사이 운영 쪽도 계속 개발해서, **지금 `origin/main` 의 관리자 `app.js` 는
32,722줄로 오히려 늘었습니다.** 우리 쪽은 기능이 같은 코드를 60개 파일에
나눠 담고 있습니다.

브랜치 `stabilize/phase-0`. 우리 커밋 80개 남짓에 `origin/main` 병합이 8번
들어 있습니다(정확한 수는 `git rev-list --count origin/main..HEAD`).
**아직 어디에도 푸시하지 않았습니다.** 현재 `1.0.382` 까지 따라잡은 상태입니다.

---

## 왜 했나 — 코딩 에이전트가 파일을 읽을 수 없었습니다

관리자 `app.js` 는 **3만 줄이 넘고 약 46만 토큰**입니다. 어떤 모델도 한 번에
못 읽습니다. 그래서 에이전트가 일할 때 이런 일이 벌어졌습니다.

- **"이 함수 어디 있지" 를 매번 검색으로 찾아야 합니다.** 파일을 열 수 없으니
  `grep` 으로 줄 번호를 알아내고 그 언저리만 잘라 읽습니다.
- **주변 맥락을 못 봅니다.** 그 함수가 어떤 함수들과 한 덩어리인지, 비슷한 일을
  하는 게 이미 있는지 알 수 없습니다. **그래서 중복이 쌓입니다.**
- **고칠 때 영향 범위를 모릅니다.** 누가 이 함수를 부르는지 확인하려면 또 검색입니다.
- 결과적으로 **탐색에 시간이 다 갑니다.**

지금은 관리자 `app.js` 가 **1,115줄, 약 1.2만 토큰**입니다. 통째로 읽힙니다.

### 구체적으로 무엇이 달라지나

"이용권 잔여 판정을 고쳐야 한다" 는 상황을 예로 들면 이렇습니다.

| | 작업 전 | 지금 |
|---|---|---|
| 어디를 볼까 | `app.js` 3만여 줄 안 어딘가 | `domain/tickets.js`(964줄) — 판정 규칙은 여기 |
| 화면 표시도 고쳐야 하면 | 같은 파일 안 어딘가 | `views/tickets.js`(236줄) |
| 서버에서 가져오는 부분은 | 같은 파일 안 어딘가 | `data/` |
| 읽어야 할 양 | 파일을 못 여니 검색으로 더듬기 | 해당 파일 하나 |

**폴더 이름이 곧 "무엇을 하는 코드인가" 입니다.** 어디를 열지 정하는 데
검색이 필요 없습니다.

---

## 어떻게 구조화했나

### 파일 하나 → 역할별 폴더

| 폴더 | 넣는 것 |
|---|---|
| `domain/` | 값을 받아 **판정해 돌려주는** 것. DOM 도 서버도 만지지 않습니다 |
| `views/` | **화면을 그리는** 것 (`render*`, HTML 문자열 생성) |
| `events/` | **리스너 등록.** `bindEvents` 를 쪼갠 것 |
| `data/` | **서버(Supabase)** 에 붙는 것 |
| `actions/` | **사용자가 누른 것**을 처리 (폼 제출·저장·요청) |
| `forms/` | **폼 항목·표시를 맞추는** 것 (선택지 채우기, 필드 숨기기). 서버는 안 부릅니다 |
| `ui/` | **모달·시트·토스트**를 여닫는 것 |
| `storage.js` | `localStorage` 읽기·쓰기 |
| `catalog.js` | 화면에 쓰는 **고정 데이터 표.** 리터럴만이고 계산도 호출도 없습니다 |
| `settings.js` | 저장소 키·버전 문자열·크기 |
| `app.js` | `state`, 부팅 코드, `bindEvents`, 시작점. **그 외에는 아무것도** |

폴더 안은 **주제별**로 다시 나눴습니다. 관리자 기준으로 이렇습니다.

```
domain/   billing coaches common lessons members payment policy record schedule tickets values
views/    billing common dashboard data makeup members notes reports schedule settings tickets
data/     billing common member schedule settings
forms/    coach common member policy schedule tickets
actions/  billing coach common member notice report schedule settings
ui/       billing common member schedule settings
events/   billing common data delegated members notes reports schedule settings
```

즉 **"결제 관련 판정" 은 `domain/billing.js`, "결제 화면" 은 `views/billing.js`,
"결제 서버 호출" 은 `data/billing.js`** 로 갈립니다.

### 규모가 이렇게 바뀌었습니다

| | 파일 수 | 합계 | 평균 |
|---|---|---|---|
| 관리자 | 60개 | 37,315줄 | 621줄 |
| 회원앱 | 58개 | 15,321줄 | 264줄 |
| 코치앱 | 43개 | 8,151줄 | 189줄 |

가장 큰 파일이 **`domain/common.js` 2,844줄**입니다. 이보다 큰 것은 둘뿐인데,
`curriculum-catalog.js` 4,562줄은 순수 데이터이고 `schedule-v2-admin.js` 4,196줄은
아래 3번에서 따로 다룹니다.

### 이름으로 가르지 않고 동작으로 갈랐습니다

`sync*`·`render*` 같은 이름 규칙으로 나누면 틀립니다. 실제로 관리자의 `sync*`
48개 중 **서버를 부르는 것은 5개뿐이고 33개는 폼 필드를 맞추는 일**이었습니다.
그래서 기준을 이렇게 잡았습니다.

```
서버(Supabase)를 부르는가            → data/
localStorage 를 만지는가              → storage.js
HTML 문자열을 만드는가                → views/
DOM 을 만지고 이름이 open/close/toggle → ui/
DOM 을 만지는가                       → forms/
아무것도 안 하는가                    → domain/
```

**폴더 이름이 실제와 다르면 이어받는 사람이 엉뚱한 파일을 엽니다.** 실제로 `domain/` 에 화면을
그리는 함수가 섞여 있어 8개를 도로 옮긴 적이 있고, 같은 주제인데 파일이
`member.js`/`members.js` 로 갈려 있던 것도 합쳤습니다.

### 세 앱이 같은 구조가 되면서 중복이 드러났습니다

회원앱과 코치앱에 **글자까지 같은 함수 20개(249줄)** 가 있었습니다. 한쪽만
고치면 다른 쪽이 깨진 채 남는 구조였습니다. `app/shared/` 로 합쳐서
**지금 두 앱 사이 중복은 0** 입니다.

`escapeHtml` 은 따로 다뤘습니다. 세 앱에 사본이 셋이었는데, CSP 를 조이지 못한
지금 **유일한 XSS 방어선**이라 누가 강화하면 한 앱만 고치고 끝날 위험이
있었습니다. `app/shared/tennisnote-escape-html.js` 하나로 모았습니다.

---

## 어떻게 안전을 담보했나

이 작업의 핵심입니다. **"조심했다" 가 아니라 증명입니다.**

1. **함수 본문을 한 글자도 바꾸지 않고** 옮깁니다.
2. **되돌리기 검증** — 뗀 것을 제자리에 도로 넣으면 원본과 바이트 단위로 같은가.
   같으면 내용이 안 바뀌었다는 증명입니다.
3. `git show HEAD:app.js` 와 대조해 **개수·본문·유실·중복** 확인.
4. `./scripts/verify.sh` (검사 271개).
5. **브라우저로 직접 눌러봅니다.**

이 방식으로 옮긴 코드에서 **동작이 달라진 적은 한 번도 없습니다.** 실수는
옮기는 과정(로드 순서, 등록 누락)에서 났고 전부 검사기나 브라우저가 잡았습니다.
숨기지 않고 [docs/incidents.md](docs/incidents.md) 에 다 적어뒀습니다.

반대로 예전에 시도했던 "기본값 이음매"(전역을 매개변수로 바꾸는 방식)는
시그니처가 바뀌어 바이트 대조를 할 수 없었고, **브라우저에서야 드러나는 사고를
세 번 냈습니다.** 61개가 남은 상태에서 중단했고, 지금은 쓰지 말라고 문서에
적어뒀습니다.

### 검사기를 늘린 방식

**검사기가 못 잡은 사고가 나면, 고치기 전에 검사기부터 고쳤습니다.**
그래서 126개에서 271개가 됐습니다. 실제로 이 검사들이 작업 중에 여러 번
잡았습니다 — 예를 들어

- 버전 문자열이 새 파일로 옮겨가 갱신 스크립트가 놓친 것
- 상수를 옮기다 로드 순서가 꼬여 터진 것 (두 번)
- `bindEvents` 가 엉뚱한 폴더로 간 것
- 공용 파일을 관리자에 실으면 `$` 가 충돌해 페이지가 죽는 것

사고와 그것을 막는 검사는 [docs/incidents.md](docs/incidents.md) 에 짝지어 뒀습니다.

---

## 먼저 코덱스 쪽에 물어볼 것

**아래 답이 나와야 그다음 "정해야 할 것" 을 정할 수 있습니다.**
코덱스는 클라우드에서 도는 것으로 보입니다(브랜치 이름이 `codex/…-20260821`,
커밋이 새벽에도 일정한 간격). 그래서 **"로컬에서 확인" 이라는 전제 자체가
성립하지 않을 수 있습니다.**

### 1. 저장소에 둔 지침 문서를 읽습니까?

> "저장소 루트에 `AGENTS.md` 를 두면 작업 전에 읽습니까?
>  아니면 매번 요청에 규칙을 같이 적어야 합니까?"

**왜 묻나** — 지금 `origin/main` 에는 **지침 파일이 하나도 없습니다.**
`AGENTS.md` 도 `CLAUDE.md` 도 없습니다. 즉 코덱스는 지금 아무 규칙 없이
일하고 있고, 우리가 쓴 문서를 읽는지 아무도 확인한 적이 없습니다.

| 답 | 그러면 |
|---|---|
| 읽는다 | 문서로 규칙을 세울 수 있습니다. 지금 만든 `AGENTS.md` + `docs/` 가 그대로 작동합니다 |
| 안 읽는다 | **문서는 사람용일 뿐입니다.** 규칙은 CI 와 브랜치 보호 같은 **구조로만** 강제해야 합니다 |

**이게 제일 중요한 질문입니다.** 답에 따라 인수인계의 성격이 달라집니다.

### 2. 브라우저를 띄워서 화면을 눌러볼 수 있습니까?

> "작업한 화면을 브라우저로 직접 열어서 눌러보고 확인할 수 있습니까?"

**왜 묻나** — 정리하다 낸 사고 3건이 **브라우저에서만** 드러났습니다.
자동 검사는 로드 시점과 정적 구조만 봅니다.

| 답 | 그러면 |
|---|---|
| 된다 | [docs/verification.md](docs/verification.md) 절차를 그대로 씁니다 |
| 안 된다 | **화면 확인을 할 사람이 없습니다.** 자동 검사가 유일한 방어선이 되므로 `verify.sh` 를 CI 에 넣는 것이 선택이 아니라 필수가 됩니다. 그리고 **누가 눈으로 볼지**를 따로 정해야 합니다 |

### 3. 테스트 명령을 돌릴 수 있습니까?

> "`node --test` 와 `python3` 로 된 검사 스크립트를 돌릴 수 있습니까?
>  인터넷 연결 없이 돌아갑니다."

**왜 묻나** — `verify.sh` 는 `node` 와 `python3` 만 있으면 되고 네트워크가
필요 없습니다. 대개 될 것이라 보지만, 안 되면 **CI 에만 의존**해야 합니다.

### 4. PR 병합을 누가 누릅니까? 자동입니까?

> "PR 이 올라가고 1분 안에 병합되던데, 사람이 누르는 겁니까 자동입니까?
>  자동이면 끌 수 있습니까?"

**왜 묻나** — 병합이 곧 실서비스 배포입니다. 새벽 0시 20분에도 52초 만에
병합됐습니다. **사람이 보고 있지 않다는 뜻입니다.**

| 답 | 그러면 |
|---|---|
| 자동이고 끌 수 있다 | "운영자가 요청할 때만 배포" 로 바꿀 수 있습니다 |
| 자동인데 못 끈다 | `main` 에 보호 규칙을 걸어 **바깥에서** 막아야 합니다 |
| 사람이 누른다 | 지금도 승인 절차가 있는 셈이니, 무엇을 보고 누르는지 확인하세요 |

### 5. 기본 브랜치를 `dev` 로 바꾸면 거기서 작업합니까?

> "기본 브랜치를 `dev` 로 바꾸면 코덱스도 `dev` 에서 작업을 시작합니까?
>  `main` 에 직접 푸시 금지 규칙을 걸면 작업이 막힙니까?"

**왜 묻나** — `dev` 안이 성립하려면 이게 되어야 합니다. 안 되면 `dev` 는
우리 쪽 작업 공간일 뿐이고 배포는 그대로 자동으로 나갑니다.

### 6. 버전 올리기를 스크립트로 바꿔도 됩니까?

> "버전을 올릴 때 `./scripts/bump_release.py --next` 를 쓰도록 바꿔도 됩니까?
>  지금은 여러 파일 344곳을 손으로 맞추고 있습니다."

**왜 묻나** — 한 곳만 빠뜨리면 그 파일만 옛 캐시에 남습니다.
실제로 그래서 **관리자만 옛 커리큘럼을 본 사고**가 있었습니다.

---

### 7. 작업 기록을 저장소에도 남길 수 있습니까?

> "작업 기록을 옵시디언에 남기시던데, **나중에 찾아볼 것만** 저장소에도
>  남길 수 있습니까? 커밋 메시지 본문이나 PR 설명이면 충분합니다."

**왜 묻나** — 지금 저장소만 보면 **"왜 이렇게 고쳤는지" 를 알 수 없습니다.**
커밋의 2/3 가 `Publish Tennis Note PWA 1.0.x` 라 제목에 정보가 없고, 이유는
전부 저장소 밖에 있습니다. 이어받는 쪽에서는 볼 수 없는 곳입니다.

옵시디언을 그만두자는 게 아닙니다. **되돌아볼 사람이 볼 수 있는 곳에도
한 줄 남기자**는 것입니다.

---

## 작업 기록의 틀 (제안)

옵시디언 파일이 세션마다 하나씩 쌓여 **사람이 훑기 어려운 상태**로 보입니다.
파일 수를 줄이는 것보다 **갈래를 나누는 것**이 낫습니다.

### 나중에 찾아보는 것은 셋뿐입니다

이 저장소에서 실제로 다시 꺼내 본 기록이 이 셋입니다. **이미 자리가 있습니다.**

| 기록 | 어디에 | 왜 저장소여야 하나 |
|---|---|---|
| **왜 이렇게 고쳤나** | 커밋 본문 · PR 설명 | `git log` 로 코드 옆에서 같이 읽힙니다 |
| **무엇이 터졌고 무엇이 막나** | [docs/incidents.md](docs/incidents.md) | 사고와 검사가 짝지어 있어야 "이 규칙 없애도 되나" 를 판단할 수 있습니다 |
| **아직 안 된 것** | [docs/unfinished.md](docs/unfinished.md) | 다음 사람이 제일 먼저 여는 곳입니다 |

나머지(진행 중 메모, 시도했다 버린 것, 중간 계산)는 **옵시디언에 그대로
두면 됩니다.** 찾을 일이 거의 없습니다.

### 옵시디언 쪽은 파일 이름만 바꿔도 됩니다

**목록 자체가 색인이 되게** 만드는 것이 핵심입니다. 열지 않고 훑을 수 있어야
합니다.

```
2026-08-21  1.0.382  결제 화면에서 앵커가 안 잡히던 것.md
2026-08-21  1.0.381  보강 신청 마감 시간 처리.md
2026-08-20  1.0.377  코치 정산 미리보기 중복 선언.md
```

`날짜 · 버전 · 무엇을 했는지 한 줄` — 이것만으로 "그때 그 결제 건" 을
파일 목록에서 바로 찾습니다.

그리고 **파일 맨 위 세 줄을 고정**하면 열었을 때 3초 안에 판단됩니다.

```
- 무엇을:
- 왜:
- 확인한 것:
```

**이 세 줄이 곧 커밋 본문입니다.** 옮겨 적을 것도 없이 그대로 쓰면 됩니다.

⚠ 틀을 늘리지 마세요. 항목이 많아지면 채우다 지쳐서 아무도 안 씁니다.
**세 줄이면 충분합니다.**

---

## 정해야 할 것 세 가지

### 1. 이 구조를 채택할지

채택하면 `main` 으로 보내고, 아니면 브랜치를 버립니다.
**결정 전에는 `main` 에 넣지 마세요.** `main` 은 푸시가 곧 배포입니다(아래 2번).

채택할 경우 **같이 지워야 할 것**이 있습니다. `docs/merging.md` 와
`scripts/merge_report.py` 는 "저쪽은 `app.js` 한 덩어리, 우리는 쪼갠 파일" 이라는
격차를 푸는 도구입니다. 구조가 `main` 에 들어가면 그 격차가 사라지므로
**남겨두면 낡은 문서가 아니라 틀린 지시가 됩니다.**

### 2. 브랜치·배포 정책

#### 지금 어떻게 되어 있나 (확인한 사실)

**코덱스가 커밋하면 사람 손을 거치지 않고 약 1분 뒤 실서비스에 나갑니다.**

| 버전 | `Publish` 커밋 | PR 병합 | 간격 |
|---|---|---|---|
| 1.0.376 | 21:23:19 | 21:24:36 | 77초 |
| 1.0.377 | 23:15:25 | 23:16:28 | 63초 |
| 1.0.378 | **00:20:26** | **00:21:18** | 52초 |
| 1.0.379 | **01:20:01** | **01:20:56** | 55초 |
| 1.0.382 | 10:36:23 | 10:37:28 | 65초 |

새벽 0시 20분과 1시 20분에도 간격이 같습니다. 병합되면 `main` 에 push 가 나고,
`deploy-cloudflare-pages.yml` 이 `pages deploy … --branch=main` 즉 **프로덕션**에
올립니다.

그 1분 동안 도는 검사는 이것뿐입니다.

```
node --check                파일 4개 — 문법만
build_cloudflare_pages.py   빌드가 되나
check_cloudflare_build.py   버전·캐시 표시, 링크
```

**테스트가 없습니다.** `origin/main` 에는 `tests/` 폴더도 `verify.sh` 도 아예
없습니다. 지금의 검사 271개는 **전부 이 브랜치에만** 있습니다.
그래서 이용권 판정이 틀려도, 리스너 등록이 빠져도 **1분 뒤 사용자에게 나갑니다.**

⚠ 병합을 누른 것이 사람인지 자동화인지는 저장소만으로 확정할 수 없습니다
(커미터가 `GitHub` 인 것은 UI 클릭과 API 호출이 같습니다). 다만 새벽에도
52~77초로 일정한 간격은 사람의 리듬이 아닙니다. **일요일에 확인하세요.**

#### 제안 — 개발은 `dev`, 배포는 요청받을 때

1. 기능·수정은 전부 `dev` 에 쌓습니다. **여기서는 배포가 일어나지 않습니다.**
2. 운영자가 "올려주세요" 할 때만 버전을 올리고 `dev` → `main` PR 을 엽니다.
3. **그 PR 에서 `verify.sh` 가 돌고, 통과해야 병합됩니다.**
4. 병합되면 배포됩니다.

이렇게 하면 `main` 의 뜻이 하나로 정해집니다 — **`main` = 지금 배포된 것.**
그리고 `main` 커밋 목록이 곧 릴리스 목록, `dev` 커밋 목록이 곧 변경 목록이
됩니다. 지금 한 커밋에 뭉쳐 있어 추적이 안 되던 것이 그대로 풀립니다.

##### ⚠ 테스트는 병합 **전**이어야 합니다

"`main` 에 머지하고 테스트 돌리고 배포" 순서로는 늦습니다. 배포는 `main` 에
push 가 나는 순간 걸려 있어서, **머지한 시점에 이미 배포가 시작됩니다.**
테스트가 그 뒤에 실패해도 되돌리는 것 말고는 할 수 있는 게 없습니다.

GitHub 에 있는 기능으로 해결됩니다.

- CI 를 `pull_request` 에서도 돌린다 (저쪽 CI 는 이미 그렇게 되어 있습니다)
- `main` 에 **"검사 통과 필수"** 보호 규칙을 건다

그러면 **병합됐다 = 검사를 통과했다** 가 구조로 보장됩니다.
문서로 부탁할 필요가 없어집니다.

##### 로컬 브라우저 확인은 자동 검사를 대신하지 못합니다

브라우저 확인은 꼭 필요합니다 — 정리하다 낸 사고 3건이 거기서만 드러났습니다.
다만 **기록이 남지 않습니다.** 에이전트가 "확인했습니다" 라고 하면 운영자는
확인할 방법이 없습니다. `verify.sh` 는 통과 여부가 PR 에 남습니다.

둘은 대체재가 아니라 순서입니다. **검사가 먼저, 브라우저가 그다음.**

❓ **일요일에 꼭 물어볼 것: 코덱스가 브라우저를 띄울 수 있습니까?**
못 띄우면 "로컬에서 확인" 은 사실상 안 하는 것이 됩니다. 그러면 자동 검사가
유일한 방어선이 되므로 아래 안건이 훨씬 더 중요해집니다.

##### `dev` 에도 CI 를 걸어야 합니다

지금 CI 는 `pull_request` 와 `main` push 에만 걸립니다. `dev` 에 그냥 푸시하면
**아무 검사도 안 돌아갑니다.** 트리거에 `dev` 를 추가하세요.

##### 급할 때와 되돌릴 때

- **급한 수정** — `dev` 에 끝나지 않은 작업이 섞여 있으면 거기서 올릴 수 없습니다.
  `main` 에서 딴 hotfix 브랜치로 가세요.
- **되돌리기** — 병합이 곧 배포이므로, `main` 에서 revert 하면 이전 상태가
  그대로 배포됩니다. 별도 절차가 필요 없습니다.

#### 별도 안건: `verify.sh` 를 저쪽 CI 에 넣기

구조 채택과 **따로 이야기할 값어치가 있습니다.** 지금 프로덕션으로 가는 관문이
문법 검사뿐이라는 것은, 구조를 어떻게 하든 남는 문제입니다.

다만 완전히 분리되지는 않습니다. **검사 16개 파일 중 10개가 나눈 폴더에서 함수를
직접 읽어옵니다.** `app.js` 한 덩어리에서는 그대로 돌지 않습니다. 그래서 실제로는

- 구조를 채택하면 → 검사 271개가 **딸려 옵니다**
- 채택하지 않으면 → 6개(버전·전역 이름·리스너 등록·이용권 판정·문서·환경변수)만
  거의 그대로 쓸 수 있고, 나머지는 다시 써야 합니다

**"검사를 붙이고 싶다" 가 구조를 채택할 이유 중 하나입니다.**

#### 물어볼 것: 운영 쪽 에이전트도 `dev` 를 쓸 수 있는가?

- **쓴다면** — `dev` 를 기본 브랜치로 하고 `main` 에 보호 규칙(직접 푸시 금지)을
  걸면 **구조로 막힙니다.** 문서로 부탁하는 것보다 확실합니다.
- **안 쓴다면** — `dev` 는 우리 쪽 작업 공간일 뿐이고, `main` 의 뒤섞인 커밋을
  머지하는 부담은 그대로입니다. 배포와 개발이 분리되는 이득만 남습니다.

### 3. `schedule-v2-admin.js` (4,196줄) 를 어떻게 할지

**분석해봤고, 지금은 그대로 두는 쪽을 권합니다.**

이 파일은 나머지와 성격이 다릅니다. 전체가 IIFE(즉시실행함수) 하나로 감싸여
있고, 밖으로는 딱 두 개만 내놓습니다.

```js
(() => {
  … 함수 178개 …
  window.TennisNoteScheduleV2Comparison = { compare, baselineStatus };
  window.TennisNoteScheduleV2Admin = Object.freeze({ openLesson });
})();
```

`app.js` 는 모든 함수가 전역이라 다른 파일로 옮겨도 서로 계속 보였고, 그래서
**호출부를 한 줄도 안 바꾸고** 옮길 수 있었습니다. 여기는 함수들이 IIFE 안에
갇혀 있어서 그게 안 됩니다.

확인한 것들:

| 확인 | 결과 |
|---|---|
| 바깥 전역에 의존하는가 | **아니오.** `window.TennisNote*` 로 명시적으로만 읽습니다 |
| 로드 시점에 누가 부르는가 | **아니오.** 클릭 시점(`openLessonRecordModal`)에만, 그것도 옵셔널 체이닝 |
| `TennisNoteScheduleV2Comparison` | **관리자 어디서도 안 씁니다.** 다른 데서 쓰는지는 이 저장소만 봐서는 알 수 없습니다 |

즉 나누려면 **ES 모듈**(`import`/`export`)로 가야 하는데, 기술적으로는 됩니다.
번들러도 `package.json` 도 필요 없고, 로드 시점 의존이 없으니 실행이 뒤로 밀려도
깨질 지점이 없습니다.

**그런데도 권하지 않는 이유**는 하나입니다. 178개 함수에 `import` 를 붙여야 하니
**본문이 바뀌고, 바이트 대조로 증명할 수 없습니다.** 지금까지 동작이 달라진
적이 없다고 말할 수 있는 근거가 그 증명입니다. 게다가 `script-load` 검사가 클래식 스크립트만 다뤄서
검사 범위 밖으로 나갑니다.

얻는 것은 "파일이 작아진다" 뿐인데, 이 파일은 **역할이 하나로 명확하고 이미
경계가 그어져 있어** `app.js` 가 겪던 문제를 겪고 있지 않습니다.

**나중에 이 판단을 뒤집을 만한 이유**: 운영 쪽이 이 파일을 자주 고쳐야 해서
4,196줄이 실제로 걸림돌이 되는 경우. 그때 ES 모듈로 가면 됩니다.

---

## 인수인계 방법

### 문서

| 파일 | 무엇 |
|---|---|
| [AGENTS.md](AGENTS.md) | 진입점. 절대 규칙 + "이 작업엔 이 문서" 표 |
| [CLAUDE.md](CLAUDE.md) | AGENTS.md 를 가리키는 포인터 (규칙을 두 벌 두면 어긋나므로) |
| [docs/structure.md](docs/structure.md) | 폴더가 뜻하는 것 · 배치 · 앱별 로드 방식 차이 |
| [docs/verification.md](docs/verification.md) | 검사기가 각각 막는 것 · 실패 시 대처 |
| [docs/splitting.md](docs/splitting.md) | 파일 쪼개는 절차와 증명 방법 |
| [docs/merging.md](docs/merging.md) | `origin/main` 병합 (채택하면 삭제) |
| [docs/releasing.md](docs/releasing.md) | 버전 올리기 |
| [docs/incidents.md](docs/incidents.md) | 사고 ↔ 그것을 막는 검사 |
| [docs/unfinished.md](docs/unfinished.md) | 아직 안 된 것 · 알려진 버그 |

문서 자체도 검사합니다 — 링크·앵커가 깨지거나, `CLAUDE.md` 에 규칙이 다시
새거나, `docs/` 에 안내 안 된 문서가 생기면 `tests/docs-links.test.js` 가 잡습니다.

### 코드

브랜치를 푸시하지 않았으므로 **번들 파일**로 옮깁니다.

```bash
git clone -b stabilize/phase-0 tennis-note-stabilize-phase-0.bundle tennis-note-public
cd tennis-note-public
git remote set-url origin https://github.com/leisurewang0827/tennis-note-public.git
git fetch origin
git config user.name "dev.jsds" && git config user.email "dev.jsds@gmail.com"
./scripts/verify.sh
```

`python3` 와 `node` 만 있으면 됩니다. `npm install` 은 필요 없습니다.

---

## 남은 것

[docs/unfinished.md](docs/unfinished.md) 에 정리돼 있습니다. 요약하면 이렇습니다.

- `schedule-v2-admin.js` 4,196줄 (위 3번)
- 기본값 이음매 61개 — 새로 쓰지 말고, 만질 때만 규칙을 지킬 것
- 남은 중복 3개(13줄) — 짝을 이뤄 고쳐야 함
- `terms.html` 없음, 개인정보처리방침 두 벌
- `remaining` 이 `NULL` 이면 이용권이 "소진" 으로 판정되는 버그
  (지금은 안 터지지만 DB 에 `NULL` 행이 생기면 그 회원은 예약 불가)

### 검사기로 못 막는 것

**`escapeHtml` 누락이 유일하게 문서와 습관에만 기대는 규칙입니다.**
`verify.sh` 를 통과해도 감싸지 않은 값이 그냥 지나갑니다. CSP 의 `script-src` 를
아직 조이지 못했으므로 이스케이프가 유일한 방어선이고, 실제로 회원 이름이
관리자 화면에서 실행되는 취약점이 있었습니다.
